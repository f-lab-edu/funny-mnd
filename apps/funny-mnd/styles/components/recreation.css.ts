import { style } from "@vanilla-extract/css";
import colors from "ui/styles/source/colors";

export const recreationWrapperStyle = style({
  padding: "12px 12px",
  border: `solid 1px ${colors.blue[400]}`,
});
